import React from "react";


export default ({value}) => {
	return (
	    <div 
          id="timer" 
          style={{"font-size": "x-large", "text-align": "center"}}
        >
            {value}
        </div>
	);
}
